# Project Instructions

- ゴールから外れる提案をしないでください。
- ゴールに進む提案を必ずしてください。
- 回答には必ず「次のタスクはこれ」「今の進捗を全体像から整理するとこれ」を含めてください。
- 私が大学生だと思って、言語化してください。
<<<<<<< HEAD

## 「商品の品質」より「売り方（伝わり方）の品質」が結果を決める

> 正本: `../web-ios-android/CLAUDE.md` の設計方針（2026-09-01 確立）。ここは要点だけ持つ。

明らかに中身が劣る商品でも売れていて、良いものを作っているのに売れない、という現象がある。
差がついているのは**中身ではなく、伝わり方**であることが多い。

**このリポで守ること:**

- 実装が終わっただけで「できました」と報告しない。**それが実際にユーザーへ届く形
  （言葉・見た目・導線）になっているか**まで自分の責任範囲として見る。
- 機能が正しく動くことと、その機能に気づいてもらえることは別。
  作った本人には見えているものが、初見の人には見えていない。
- LPは特にここが結果に直結する。**実機で開いて、初見の人がどう受け取るかまで確かめる。**

★実際に起きた例（2026-09-01・すべてこのリポ）:

| 事象 | 中身 | 伝わり方 |
|---|---|---|
| LPのメニュー | 5ページ分の導線を実装済み | **1100px以下でハンバーガーに畳まれ**、存在に気づかれなかった |
| フッターのロゴ | 正しい画像を配信していた | **ブラウザのキャッシュ**で古いロゴ（別ブランド）が表示され続けた |
| 動画の実演パート | 映像は正常 | **音量が他より15dB低く**、実質聞こえていなかった |

いずれも「機能は正しい」が「伝わっていない」型。
**動くことを確かめて終わりにせず、実際に見える状態まで見ること。**

## LPを触るときの決まり

- ヘッダー・フッターは `apps/lp/site-chrome.js` の1箇所で管理する。
  ページごとに個別実装しない（金型: `../web-ios-android/templates/web/site-chrome/`）。
- 画像を差し替えたら**URLの `?v=` を上げる**。ファイル名が同じだと、
  以前見た人のブラウザは古い画像を掴んだまま更新されない（2026-09-01 実際に発生）。
- 公開前に `node scripts/check-lp-links.mjs` を通す。
- 本番へ出したあと、**実機で開いて目で見る**。HTTP 200 は「ページが返った」だけで
  「中身が出ている」ではない。
=======
- L Harness Proxy から担当者として1対1返信する場合は、`X-Line-Harness-Source: manual` を必ず付けてください。予約通知などの自動送信には付けないでください。
- Google Meetの個別相談を確定・変更した場合は、カレンダー更新だけで終えず、`POST /api/meet-consultations` にGoogle Calendar event ID・LINE friend ID・日時・Meet URLを登録してください。前日・1時間前のLINEリマインドを必須セットにします。キャンセル時は `DELETE /api/meet-consultations/:externalEventId` も実行してください。
>>>>>>> upstream/main
