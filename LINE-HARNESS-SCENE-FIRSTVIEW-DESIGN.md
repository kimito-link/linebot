# 設計書 — line-harness-scenes ファーストビュー改善

> 素材集め=会議ハーネス（無料枠クラウドLLM、3/5体が有効回答） ／ 統合・裏取り=司令塔Claude ／ 2026-08-17
> 対象（作成当時）: apps/lp/line-harness-scenes/index.html
>
> **【2026-08-20 追記】** この改善内容（ファーストビューのミニ会話デモ等）は、その後
> `kimitotalk.link`本体への統合に伴い `apps/lp/kimitotalk/index.html` に移設済み。
> `line-harness-scenes`ディレクトリ自体は削除されている。設計判断・実装内容は変わらず有効。

## 司令塔による統合判断

会議3体（gpt-oss-120b・qwen3.6-27b・gpt-oss-20b）は「抽象的なLINEチャットUIフレームをファーストビューに前倒しし、業種選択で具体例に分岐させる」という方向で一致した。**この方向性（抽象チャットフレームの前倒し）は採用する**が、実装手段（業種選択モーダル・JS）は不採用にする。

**不採用の理由**:
- モーダル・業種選択UI・JSロジックの新規実装は、静的HTML+最小CSSという現在の技術方針からの逸脱であり、スコープ拡大になる（今日の「GPTお引越しパック」反省——過剰実装より先に訴求の骨格を固める、という進め方と矛盾する）。
- 姉妹LP`gpt-hikkoshi/index.html`が既に`.chat-demo`/`.chat-msg`というCSSクラスで**静的な擬似チャットデモを実装済み**（JS不要）。これをそのまま流用すれば、会議が懸念した「見た目が粗い」問題を回避しつつ、モーダルなしで同等の効果が得られる。

**採用する代替案**: 業種を1つに固定せず、**複数業種の短い会話を並べて見せる**（クリニック・専門知見・コミュニティ等から1〜2往復ずつ抜粋）。これにより「1業種に絞って他業種の離脱を招く」という会議のQ3懸念（3体とも言及）を、モーダルという新規UIなしで解消できる。

## 1. 結論

ファーストビューに、**複数業種を横断する短い会話デモ**（`gpt-hikkoshi`と同じ`.chat-demo`スタイルを流用、業種選択の切り替えUIなし）を追加する。テキストは現状維持に近い形とし、CTAは変更しない（LINE OA未発行の状態を継続して正直に表示）。

## 2. 根拠

- 会議の核心的な洞察（抽象的でもチャットUIをファーストビューに見せると価値が直感的に伝わる）は採用する。
- 会議のQ3懸念（1業種の具体例だと他業種が離脱する）への対処は、モーダルによる動的分岐でなく、**複数業種を並べて見せる静的な方法**で解決する。2〜3業種のミニ会話を横に並べれば、閲覧者は自分の業種に近いものを見つけやすく、かつ実装はCSSのみで完結する。
- 姉妹LPとのデザイン一貫性: `gpt-hikkoshi`の`.chat-demo`スタイルをそのまま流用することで、実装コストゼロで統一感のあるビジュアルになる。

## 3. 反論・リスク

- **リスク**: 複数業種を並べると、逆に1業種あたりの情報量が薄くなり「浅い」印象を与える可能性がある。
- **対処**: 2業種に絞る（クリニック予約＋専門知見、または既存の「今すぐ対応可能」バッジが付いた2シーンに合わせる）。1往復〜1.5往復程度の短い会話に留め、詳細は下のシーンカードセクションに譲る。

## 4. 具体案（実装レベル）

`apps/lp/line-harness-scenes/index.html`のheroセクション直後に、ミニ会話デモを追加する。既存CSS（`.chat-demo`/`.chat-msg`）は無いため、`gpt-hikkoshi/index.html`と同一のスタイルを追加する。

会話内容の候補（煽り表現なし、既存シーンカードと整合）:
- クリニック予約: 「明日の午後空いてますか」→「14時と15時半が空いています」
- 専門知見: 「消費税の書き方がわからない」的な短いFAQ応答（gpt-hikkoshi側のダミー例とは別内容にする）

## Assumption List

| # | 断定 | 実際の根拠状況 |
|---|---|---|
| A1 | 2業種のミニ会話で「自分に関係ある」と感じさせられる | 未検証。会議の推測を司令塔がさらに縮小した判断であり、実測データはない |
