# 設計書 — LP群を作り切るための最終構成

> 素材集め=会議ハーネス（無料枠クラウドLLM、5/6体が有効回答） ／ 統合・裏取り=司令塔Claude ／ 2026-08-17
> 前提: [LINE-HARNESS-SCENE-PRESENTATION-DESIGN.md](LINE-HARNESS-SCENE-PRESENTATION-DESIGN.md)（リスク階層化の決定）

## 司令塔による統合判断

会議は「2LP体制を維持し、`gpt-hikkoshi.html`は独立LP・即時コンバージョン、`line-harness-scenes.html`はハブ的な業種別インデックス」という構成で複数モデルが一致した（子ページへの降格・LP統合案は明示的に却下）。**この構成の骨格は採用する。**

ただし、会議の具体案の一部は**今日既に確定した制約と矛盾**するため不採用にする：

- **「メールアドレス公開・メール登録フォーム＋Slack通知」は不採用**。ユーザーは既に「メールアドレスは公開しない方針」と明確に判断済み（このセッション内の既存決定）。会議はこの経緯を知らずに提案しており、司令塔が却下する。
- **「即座にLINE OA作成手順ボタンを出す」は不採用**。LINE公式アカウントはまだ発行されていない（Developers Console側の作業がユーザー都合で中断中）。存在しないものへの案内は虚偽表示になる。
- **「gpt-stoppage.html等の追加SEOページを今日中に量産」は不採用**。これは会議が"緊急ユーザーの離脱防止"のために提案したものだが、実案件も実績もない状態でSEOページを増やすのは、今日の反省（GPTs訴求への過信）を繰り返すリスクがある。今日はスコープに含めない。

**採用する骨格**: 2LP体制維持・相互リンクの設置・`line-harness-scenes.html`側にカード形式の業種別導線を明確化。CTAは両LPとも「LINE公式アカウント準備中」の正直な表示を維持する（メール代替もLINE代替も出さない——これは今日既に確定した設計方針であり、会議はここを変えられない）。

## 具体的な作業内容（今日中に完了させる）

1. **`gpt-hikkoshi.html`のフッターに`line-harness-scenes.html`への相互リンクを追加**（「LINE Harnessでできること一覧を見る」）
2. **`line-harness-scenes.html`のフッターに`gpt-hikkoshi.html`への相互リンクを追加**（「GPTのお引越しでお困りの方はこちら」）
3. **`line-harness-scenes.html`の価格帯言及**: 現状価格情報が皆無なので、「料金の目安」的な言及を1行追加するか検討（未確定・今回は見送り候補）
4. **CTAは変更しない**（メール・LINE即時案内とも不採用のため現状維持）

## Out of Scope（会議提案のうち採用しないもの）

- メールフォーム・Slack通知連携の実装
- LINE OA作成手順への即時CTA変更
- 追加SEOキーワードページ（`gpt-stoppage.html`等）の新規作成
- 業種×課題マトリクス表（会議提案）は`line-harness-scenes.html`の既存カード形式で代替済みと判断、作り直さない
